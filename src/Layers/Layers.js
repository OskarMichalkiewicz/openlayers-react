import React from "react";
import "./layers.css";

const Layers = ({ children }) => {
	return (
                <div>
                    {children}
                </div>
        );
};

export default Layers;